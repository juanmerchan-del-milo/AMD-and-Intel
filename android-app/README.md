# AMD vs Intel APK

Proyecto Android de la aplicación AMD vs Intel.

La aplicación se abre como una app instalada en Android (APK), no como un archivo HTML en Chrome.

## APK automática
GitHub Actions compila `app-debug.apk` y lo guarda como un artifact llamado `AMD-vs-Intel-APK`.
